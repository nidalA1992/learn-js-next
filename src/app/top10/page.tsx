import type { Metadata } from 'next';

import { RacketsTopGrid } from '@/racket/ui/containers/RacketsTopGrid';

export const metadata: Metadata = {
  title: 'Top 10',
  description: 'Top 10 rackets',
};

export default RacketsTopGrid;
